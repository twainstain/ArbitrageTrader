global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "arb-bot"
    metrics_path: /metrics
    static_configs:
      - targets: ["bot:8000"]
        labels:
          instance: "arbitrage-trader"
